import React from "react";
export default function DeductionSorter({ scenarioId, onComplete, lessonId }) {
  return (
    <div style={{ padding: 24, textAlign: "center", color: "#475569" }}>
      <p>{/* DeductionSorter — implementation pending */}</p>
      <button onClick={onComplete} style={{ marginTop: 16, padding: "8px 20px", borderRadius: 8, background: "#1B2B5E", color: "#fff", border: "none", cursor: "pointer" }}>Continue</button>
    </div>
  );
}
